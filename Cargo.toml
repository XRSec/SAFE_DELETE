[package]
name = "safe-delete"
version = "0.1.0"
edition = "2024"
description = "Safe rm alternative that moves files and directories to the system trash"
license = "MIT"

[[bin]]
name = "rmf"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.2", features = ["derive"] }
trash = "5.2.6"
